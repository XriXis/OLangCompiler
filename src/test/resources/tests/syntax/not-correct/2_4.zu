class Main is
    var a : Integer(10)
    var b : Integer(20)

    method add() : Integer is
        return a.Plus(b)
    end
end

class MyClass is
    ths() is
        var obj : MyClass
        obj := MyClass()
        var result : Integer
        result := obj.add()
    end
end

