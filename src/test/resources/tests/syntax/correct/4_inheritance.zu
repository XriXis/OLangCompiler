class Main is
    this() is
        var dog : Dog()
        var phrase : Boolean(dog.Speak())
    end
end

class Animal is
    this()
    method Speak : Boolean => Boolean(false)
end

class Dog extends Animal is
    this()

    method Speak : Boolean is
        Console().print(1)
        return Boolean(true)
    end
end
