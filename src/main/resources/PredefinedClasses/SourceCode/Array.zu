class Array[T] extends AnyRef is
    // Constructor
    this()
    this(l: Integer)
    this(copy: Array[T], newLen: Integer)
    // Conversion
    // method toList : List
    // Features
    method Length : Integer
    // Access to elements
    method get(i: Integer) : T
    method set(i: Integer, v: T)
end
