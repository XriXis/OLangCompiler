class t[T] is
    var val: T

    this() is end

    this(smth: T) is
        val := smth
    end

    method getVal(): T is
        return val
    end
end


class b extends t[Integer] is end

class main is
    this() is
        Console().print(b(7).getVal().Plus(6).toString())
    end
end
