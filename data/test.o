class Main is
    this() is
        var n: Integer
        var numbers : List[Integer]
        numbers.append(10)
        numbers.append(20)
        numbers.append(30)
        var total : Integer(0)
        Console().print(total.ToString())
    end
end
