class Main is
    this() is
        var dog : Dog()
        dog.Speak()
    end
end

class Animal is
    this()
    method Speak : Boolean => Boolean(false)
end

class Dog extends Animal is
    method Speak : Boolean is
        Console().print(true.ToString())
        return Boolean(true)
    end
end
