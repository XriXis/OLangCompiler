class Console is
    // Constructors
    this()
    // Operators
    // method print(s: String)
    // method println(s: String)
    method print(i: Integer)
    method println(i: Integer)
    method println(i: String)
    method print(i: String)
end
